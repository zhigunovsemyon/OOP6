#pragma once
#include "obj.h"

class Message {
private:
	/*sender_ -- отправитель сообщения, reciever_ -- получатель, либо NULL,
	 * если сообщение адресовано всем*/
	// Object *sender_, *reciever_;

public:
	// Message(Object * sender, Object * reciever = nullptr)
	// 	: sender_(sender), reciever_(reciever)
	// {
	// }
};
